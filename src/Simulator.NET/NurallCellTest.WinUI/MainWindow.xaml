<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NurallCellTest.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NurallCellTest.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:sp="using:ScottPlot.WinUI"
    Title="NurallCellTest.WinUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Grid.Row="0" Click="Button_Click">Go</Button>
            <CheckBox IsChecked="{x:Bind EnableBrackets,Mode=TwoWay}" >Enable Brackets</CheckBox>
        </StackPanel>
        
        <sp:WinUIPlot Grid.Row="1" x:Name="WinUIPlot1"/>
    </Grid>
</Window>
